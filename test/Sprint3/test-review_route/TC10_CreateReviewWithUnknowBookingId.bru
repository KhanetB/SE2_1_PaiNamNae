meta {
  name: TC10_CreateReviewWithUnknowBookingId
  type: http
  seq: 12
}

post {
  url: http://localhost:3000/api/reviews
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{passenger_token}}
}

body:json {
  {
    "bookingId":"cmlqnzh3l0001356q5xvda8b3",
    "rating": 4.5,
    "comment": "{{review_comment}}"
  }
}

tests {
  test("get HTTP 404", function(){
    const status = res.getStatus();
    expect(status).to.equal(404);
  })
  
  test("get message: Booking not found", function(){
    const body = res.getBody();
    expect(body.message).to.equal("Booking not found")
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
