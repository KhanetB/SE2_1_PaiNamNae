info:
  name: TC01_EnterCorrectPassword
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/auth/verify-user
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "password":"123456789"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("get HTTP 200", function(){
          const status = res.getStatus()
          expect(status).to.equal(200)
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
