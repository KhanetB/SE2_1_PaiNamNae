info:
  name: TC03_EnterNothing
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/auth/verify-user
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("get HTTP 400", function(){
          const status = res.getStatus()
          expect(status).to.equal(400)
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
