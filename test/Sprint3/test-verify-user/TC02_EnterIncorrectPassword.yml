info:
  name: TC02_EnterIncorrectPassword
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/auth/verify-user
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "password":"987654321"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("get HTTP 401", function(){
          const status = res.getStatus()
          expect(status).to.equal(401)
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
