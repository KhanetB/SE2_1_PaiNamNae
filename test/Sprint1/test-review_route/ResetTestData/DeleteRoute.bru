meta {
  name: DeleteRoute
  type: http
  seq: 2
  tags: [
    reset
  ]
}

delete {
  url: http://localhost:3000/api/routes/admin/{{route_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
