meta {
  name: TC11_CreateReviewWithIncompleteRoute
  type: http
  seq: 13
}

post {
  url: http://localhost:3000/api/reviews
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{passenger_token}}
}

body:json {
  {
    "bookingId":"{{booking_id}}",
    "rating": 4.5,
    "comment": "{{review_comment}}"
  }
}

tests {
  test("get HTTP 404", function(){
    const status = res.getStatus();
    expect(status).to.equal(400);
  })
  
  test("get message: You can review only completed routes", function(){
    const body = res.getBody();
    expect(body.message).to.equal("You can review only completed routes")
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
